## План разработки CLI-приложения для учета финансов

### 1. Архитектура проекта

[x] Проект должен быть разделён на слои (модульная структура).
[x] Необходимо выделить слой Model — классы-модели (Transaction, Category, User).
[x] Необходимо выделить слой Service — бизнес-логика (подсчёты, аналитика, лимиты).
[x] Необходимо выделить слой Repository — работа с сохранением и загрузкой данных (файл `.txt` или `.json`).
[ ] Необходимо выделить слой UI — консольное меню с циклом `while`, обрабатывающим действия пользователя.

---

### 2. Распределение функциональности

#### Блок 1: Логика и данные (Back-end)

[x] Необходимо реализовать класс `Transaction` со следующими полями: id, сумма, категория, дата, комментарий.
[x] Необходимо создать менеджер транзакций с методами `addTransaction` и `deleteTransaction`.
[x] Должен быть реализован метод подсчёта общего баланса.
[x] Должен быть реализован метод группировки расходов по категориям.
[x] Должна быть реализована проверка превышения месячного лимита.
[x] Необходимо реализовать сохранение списка транзакций в файл.
[x] Должна быть реализована загрузка данных из файла при запуске программы.

---

#### Блок 2: Интерфейс и взаимодействие (CLI)

[ ] Необходимо реализовать главное текстовое меню (добавить доход, добавить расход, показать отчет и т.д.).
[ ] Должна быть реализована проверка корректности ввода (валидация через `try-catch` или `Scanner.hasNextDouble()`).
[ ] Вывод отчетов должен быть аккуратно отформатирован через `System.out.printf`.
[ ] Может быть реализована система ввода команд одной строкой (например: `add 500 food lunch`).

---

### 3. Дополнительные функции (для усиления проекта)

[ ] Необходимо реализовать поддержку нескольких валют (внутреннее хранение в одной валюте + метод конвертации).
[ ] Необходимо реализовать экспорт данных в CSV-файл для открытия в Excel.
[x] Необходимо реализовать консольную визуализацию расходов. Вместо настоящего графика требуется нарисовать bar chart из символов, например:
`Еда:   [##########] 500р`
`Такси: [###       ] 150р`

---

### 4. Этапы выполнения проекта

[ ] Определить структуру класса `Transaction` (финальный набор полей).
[ ] Создать репозиторий на GitHub и настроить совместную работу.
[ ] Реализовать базовую версию программы с хранением данных в `ArrayList`.
[ ] Добавить сохранение в файл.
[ ] Добавить аналитику и отчеты.
[ ] Добавить дополнительные функции (валюта, CSV, визуализация).

---